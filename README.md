install and set up Node.js and Vs code
open power shell copy "Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser" paste and run then yes and run this command "npm init -y" and "npm install express cors"
"Use this if still show request failed"//open node.js and go to VScode open new terminal the command "npm init -y" and "npm install express cors"
And Enjoy!
